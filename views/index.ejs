<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            margin-top: 50px;
        }
        input[type="text"] {
            width: 300px;
            padding: 10px;
            font-size: 16px;
        }
        button {
            padding: 10px 20px;
            font-size: 16px;
        }
		.sub{
			margin-top: 20px;
		}
		
    </style>
</head>
<body>
	<div class="short">
    <h1>URL Shortener</h1>
    <form action="/shorten" method="POST">
        <label for="url" >Enter URL:</label>
        <input type="text" name="name" id="url" placeholder="Enter your URL here" required>
        <button type="submit" class="sub">Shorten URL</button>
    </form>
</div>
</body>
</html>